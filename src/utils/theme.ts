import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: '#fff',
            contrastText: '#fff'
        },
        secondary: {
            main: '#000000',
            contrastText: '#FFFFFF'
        }
    }
});
